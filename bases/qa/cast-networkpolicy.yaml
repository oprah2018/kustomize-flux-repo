apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cast-allow-web
spec:
  podSelector:
    matchLabels:
      app: cast
  policyTypes:
    - Ingress
  ingress:
    - from:
        - ipBlock:
            cidr: 10.0.0.0/8
      ports:
        - protocol: TCP
          port: 80
